from django.test import TestCase


# Legacy placeholder moved into tests package to avoid discovery collisions
class NotificationsPlaceholder(TestCase):
    def test_placeholder(self):
        self.assertTrue(True)
